rules_version = '2';

// Bloqueia acesso público e permite apenas usuários autenticados no sistema
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}