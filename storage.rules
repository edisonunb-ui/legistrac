rules_version = '2';

// MODO DE TESTE: Permite upload imediato ignorando erros de Authorized Domains.
// Recomendado apenas durante a fase de desenvolvimento no Studio.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
