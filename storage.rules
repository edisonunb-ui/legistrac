rules_version = '2';

// Regras de acesso ao Storage - ATUALIZADAS PARA FORÇAR SINCRONIZAÇÃO
// Permite que qualquer usuário autenticado faça upload e leitura.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}