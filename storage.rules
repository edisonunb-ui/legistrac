rules_version = '2';

// Regras simplificadas para garantir o funcionamento do upload.
// Permite leitura e escrita para qualquer usuário autenticado.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}