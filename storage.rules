rules_version = '2';

// Regras de Segurança para o Firebase Storage
// Permite que qualquer usuário autenticado no sistema possa ler e escrever arquivos.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}