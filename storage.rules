rules_version = '2';

// Regras de acesso ao Storage - LIBERAÇÃO PARA USUÁRIOS LOGADOS
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}